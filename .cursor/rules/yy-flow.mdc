---
description: yy-flow 多专家协同研发工作流规则
globs: ["*"]
alwaysApply: true
---
# YY-Flow Multi-Agent Workflow

在 Cursor 会话中开发或解决任务时，主 Agent 必须担任调度中枢，遵循以下流转闭环：
1. L1/L2 研发任务强制开工门禁：先查询原任务；确无重复时通过项目级 `python .yy-flow/skill/scripts/quick_task.py create` 建卡并推至【进行中】（在 Skill 仓库内使用 `python scripts/quick_task.py`）；
2. 串行调度对应专家子代理（`.cursor/agents/`）：
   - 开发实现：`@flow-dev` (李开发)
   - 凭证扫描与审查：`@flow-reviewer` (周审查，只读)
   - 测试验证与矩阵覆盖：`@flow-qa` (章测试，只读)
   - 交付与验收推进：`@flow-pm` (严经理)
3. 状态落库必须经由项目级 `python .yy-flow/skill/scripts/transition_task.py` 严格校验（在 Skill 仓库内使用 `python scripts/transition_task.py`）；
4. 详见 `.cursor/rules/yy-flow-orchestrator.mdc`；项目级安装读取 `.yy-flow/skill/SKILL.md`，直接在本仓库开发时读取根目录 `SKILL.md`。
